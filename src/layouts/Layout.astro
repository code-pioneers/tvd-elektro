---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Elektricien TVD Elektro te Denderhoutem | Elektriciteitswerken - Verlichting - Domotica - Ventilatie - Zonnepanelen - Audio - Beveiliging',
  description = 'Naast de algemene elektriciteitswerken, kunt u bij ons ook terecht voor verlichting, domotica, zonnepanelen en beveiliging van uw woning of bedrijfspand.',
} = Astro.props;
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- Favicons -->
    <link rel="icon" type="image/x-icon" href={`${base}assets/favicon.ico`} />
    <link rel="icon" type="image/png" sizes="16x16" href={`${base}assets/favicon-16x16.png`} />
    <link rel="icon" type="image/png" sizes="32x32" href={`${base}assets/favicon-32x32.png`} />
    <link rel="apple-touch-icon" sizes="180x180" href={`${base}assets/apple-touch-icon-180x180.png`} />
    <link rel="manifest" href={`${base}assets/manifest.json`} />

    <!-- Font preloads -->
    <link rel="preload" as="font" type="font/woff2" href={`${base}assets/fonts/inter-v3-latin-regular.woff2`} crossorigin />
    <link rel="preload" as="font" type="font/woff2" href={`${base}assets/fonts/inter-v3-latin-600.woff2`} crossorigin />
    <link rel="preload" as="font" type="font/woff2" href={`${base}assets/fonts/poppins-v15-latin-600.woff2`} crossorigin />

    <!-- Analytics -->
    <script
      is:inline
      async
      defer
      src="https://microanalytics.io/js/script.js"
      id="ZwSg9rf6GA"
      data-host="https://microanalytics.io"
    ></script>
  </head>
  <body>
    <slot />
  </body>
</html>
